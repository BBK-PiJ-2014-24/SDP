package classObjects

object CO2 {
  
  def main(args : Array[String]){
    
    
    var s1 = "Sally"
    var s2 = "Sally"
    
    val isSame = s1.equals(s2);
    
    if (isSame)
      println("s1 and s2 are the same")
    else
      println("s1 and s2 are NOT the same")
    
  }
  
}